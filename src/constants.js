const localhost = "http://127.0.0.1:8000";

// const usersapi = "/api";

const productsapi = "/products";

export const base = `${localhost}`;
export const productListURL = `${localhost}${productsapi}/allproducts/`;